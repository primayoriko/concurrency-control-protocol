name: C/C++ CI

on:
  push:
    branches: 
      - main
      - test_cpu_set
  pull_request:
    branches: 
      - main 

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: test
      run: make test